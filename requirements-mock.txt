# FastAPI Core (essential packages for mock mode)
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0

# Validation
pydantic==2.5.3
pydantic-settings==2.1.0

# OpenAI API (optional - mock mode works without it)
openai==1.12.0

# Utilities
httpx==0.26.0

# Note: Database packages (sqlalchemy, asyncpg, psycopg2-binary, pgvector) 
# are not needed in mock mode and can be installed later when setting up real database
